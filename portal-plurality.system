;; -*- mode:lisp -*-

(defsystem "portal-plurality"
  :serial t
  :components ((:file "package")
               (:file "lib")
               (:file "main"))
  :depends-on ("react-utilities"))
